
program ::= { statement } finish

declaration ::= "i32" [ "mut" ] ident "{" value "}"

assignment ::= ident ":=" value

finish ::= "exit" operand

statement ::= declaration | assignment

operand ::= ident | number

expr ::= term { ("+" | "-") term }

term ::= factor { "*" factor }

factor ::= number | ident